- job-template:
    name: hello-world-freestyle
    builders:
      - shell: |
          echo "Hello world!"

- job-template:
    name: hello-world-pipeline
    project-type: workflow
    dsl: |
      node {
        stage('Say hello') {
          sh """\
                echo 'Hello world!'
          """
        }
      }
